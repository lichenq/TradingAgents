# 🛠️ TradingAgents 零成本商业化 MVP 升级方案 (Zero-Cost MVP Enhancement)

本方案专门针对**“不接入付费商业接口（0 数据授权费）”**的前期状态，通过深度“榨干”现有开源/免费数据源（东方财富、雪球、百度、AkShare），通过策略层、系统层与表现层的功能创新，打造出一款具有断层式商业吸引力的最小可行性产品 (MVP)。

---

## 🧭 一、 核心设计思想：数据升华与感知倍增

既然在数据源上保持零成本，我们就必须在**“数据利用率（策略层）”**、**“交互表现力（用户层）”**、**“时效性能（系统层）”**以及**“数据持久化（存储层）”**上做足文章：
1. **数据升华**：将目前仅作为“LLM 报告素材”的东财人气榜、股吧铁杆粉丝、雪球帖子，提炼为**可量化的硬核交易因子**，作为 Stage 1 的核心过滤器。
2. **感知倍增**：利用现有 Markdown 辩论日志，通过极轻量的 Python 脚本自动渲染出具有“弹幕直播/聊天群交锋”视觉效果的本地 HTML 交互画布，让用户视觉震撼。
3. **极速并跑**：落地我们之前评估的“多进程物理隔离缓存 + Ticker independent logs”机制，将并跑限制在 3 只内，实现**无冲突、无封锁的秒级多股 Stage 2 并发研判**。
4. **统一存储**：各种分析报告、推荐结果统一存入本地 SQLite 数据库（如 `trading_agents.db`），告别散乱的文件堆积，极大提升数据结构化可用性，方便查阅与历史回溯。

---

## 🗺️ 二、 MVP 扩展功能模块清单

```
                       ┌────────────────────────────────────────┐
                       │        1. 本地网页端交互式辩论画布       │
                       │     (Web-like HTML Debate Live Showcase)│
                       └───────────────────┬────────────────────┘
                                           │
 ┌─────────────────────────────────────────┼─────────────────────────────────────────┐
 │       2. 极速无冲突安全并发引擎         │       3. 另类情绪量化选股策略超市       │
 │    (Multi-Process isolated Runner)      │   (AkShare EasternMoney Momentum Spec)  │
 └─────────────────────────────────────────┼─────────────────────────────────────────┘
                                           │
                       ┌───────────────────┴────────────────────┐
                       │       4. 零成本自动回测与复盘反思归因   │
                       │    (Automated Backtest & Post-Audit)   │
                       └───────────────────┬────────────────────┘
                                           │
                       ┌───────────────────┴────────────────────┐
                       │      5. SQLite 本地统一报告存储中心     │
                       │      (SQLite Unified Report Storage)   │
                       └────────────────────────────────────────┘
```

---

### 🎨 1. 本地网页端交互式辩论画布 (Showcase Feature)
* **背景**：Markdown 报告密密麻麻，客户付费意愿低。
* **MVP 实现（0 前端框架成本）**：
  * 编写一个轻量脚本 `scripts/generate_live_html.py`。
  * 每次 `recommend` 或 `analyze` 结束后，读取 `complete_report.md` 中的发言段落（Bull, Bear, Risk, PM）。
  * 自动匹配头像、颜色气泡，输出一个精美单文件 `debate_live.html` 并保存在结果目录下。
* **商业吸引力**：用户双击该 HTML，就能在浏览器中看到类似微信群或 Discord 直播间的动态聊天弹幕，看多派、看空派和组合经理进行多轮唇枪舌战，配上动态载入的技术指标图表，逼格瞬间拉满。

---

### 🚀 2. 极速无冲突多股安全并发引擎 (Performance Feature)
* **背景**：当前多股并跑存在全局缓存擦除冲突，串行跑多股要花半小时。
* **MVP 实现（0 数据库/代理池成本）**：
  * **进程内存隔离**：在 `run_recommend.py` 中，放弃 `ThreadPoolExecutor`，改用 `multiprocessing.Pool(processes=3)` 调起子进程，彻底实现 Python 全局变量 `_PREFETCH_CACHE` 的物理内存隔离。
  * **独立配置覆盖**：每次启动子进程，动态将 `memory_log_path` 指向该 Ticker 的独占结果文件夹中（如 `results/{ticker}/trading_memory.md`），彻底消灭文件无锁争抢冲突。
  * **限流安全阈值**：将最大并发数硬编码为 `3`，这既能将 3 只股的研判耗时压缩至 **3 分钟以内**（提速 300%），又由于并发数极低，绝对不会触发东财或雪球的 IP 封锁限制。

---

### 📊 3. 另类情绪量化选股策略超市 (Algorithmic Feature)
* **背景**：目前东财人气榜、新晋粉丝等极其优秀的筹码热度指标，只用来喂给 LLM 读，没有发挥在“量化初筛”阶段。
* **MVP 实现**：
  * 在 `run_recommend.py` 中，除现有的 `trend_pullback` 策略外，利用 AkShare 已在运行的 `fetch_stock_events.py` 中的东财热度字段，扩展新增两个免费的情绪驱动型选股策略：
    1. **东财人气爆棚策略 (`em_hot_momentum`)**：
       筛选全市场日内东财人气排名从 1000+ 暴增至前 150 名以内，且新晋粉丝占比 (热钱) 超过 55% 的高弹性、高博弈度标的。
    2. **情绪退潮杀跌避险策略 (`em_fomo_exit`)**：
       筛出技术形态虽然在支撑位，但东财人气排名 3 天内暴跌 1000 名以上、且铁杆粉丝比例急剧流失的股票，一票拦截买入冲动。
* **商业吸引力**：无需一分钱数据费，即可在包装上面向 C 端股民主打**“散户情绪反向收割因子”**和**“游资热钱追踪指标”**等极具噱头的主动投研功能。

---

### 🔄 4. 零成本每日推荐自动回测与复盘反思归因 (Closed-Loop Feature)
* **背景**：用户不仅想看今天推荐什么，更想看之前推荐的股票到底涨没涨。
* **MVP 实现**：
  * **本地推荐追踪**：每次运行 `recommend`，系统生成的推荐汇总会自动同步至 SQLite 数据库中。
  * **编写复盘脚本 `scripts/auto_backtest_audit.py`**：
    * 每天盘后，该脚本自动读取 3 天前、5 天前记录在 SQLite 数据库中的推荐记录。
    * 通过 `fetch_realtime.py` 免费接口拉取这几只股票最近几天的实际收盘价，计算“推荐后的实际收益率”。
    * **AI 归因反思**：如果某只高分推荐的股票暴跌（或踏空了暴涨股），将当时的 Agent 报告（直接从 SQLite 读取）与最近几天的实际走势输入给专门的**反思 Agent**，由其撰写一份《投研反思与策略校准报告》，并作为反思经验写入 `trading_memory.md`（或全局知识库）并更新至 SQLite 中。
* **商业吸引力**：这是机构最看重的“闭环自适应学习机制”。在商业宣传上，这代表产品具有**“每日自动纠偏、自我进化、不畏踏空与亏损”的生命力**，大大增强投研可信度。

---

### 🗄️ 5. SQLite 本地统一报告存储中心 (Storage Feature)
* **背景**：大量的 Markdown 报告、JSON 推荐汇总等散落在多层文件目录中，用户或下游服务难以极速检索、检索历史并进行多维统计。
* **MVP 实现**：
  * **数据库初始化**：引入本地 SQLite 数据库（如 `trading_agents.db`），建立 `reports`（存储 Ticker、研判时间、研判等级、AI 核心观点、Markdown/HTML 报告正文）与 `recommendations`（存储推荐时间、股票池、选股策略、推荐分数）等核心表结构。
  * **统一入库管道**：改造 `run_recommend.py` 与研判流，在报告生成、辩论结束及推荐产出时，自动将结果结构化存入 SQLite，方便后续查阅与历史追踪，取代以往单纯以零散文件形式存在。
  * **免安装/零依赖**：利用 Python 自带的 `sqlite3` 标准库，保证 0 运维成本与极致的轻量级。
* **商业吸引力**：为未来的 Web 后端管理、历史报告一键检索、策略胜率分析提供了极其稳固的数据底座，真正将“文件拼凑的玩具”升格为“可工程化运营的数据系统”。

---

## 📅 三、 极简 MVP 落地路线图 (2周极速上线)

* **第 1~3 天：落地安全并发引擎**
  * 在 `run_recommend.py` 中将 `ThreadPoolExecutor` 改为 `multiprocessing`；
  * 自动进行 `memory_log_path` 的覆写和结果目录隔离；
  * 验证 3 只股票同时并发研判无 race condition、不丢数据、不冲突。
* **第 4~7 天：开发“情绪反转”选股策略与数据提取**
  * 将 `fetch_stock_events.py` 中的 `query_sentiment` 数据直接前置并接入到 Stage 1 的量化筛选中；
  * 新增 `--strategy em_hot_momentum` 参数，支持直接在命令行和前端筛选高热度概念股。
* **第 8~10 天：HTML 互动式聊天弹幕生成器开发**
  * 编写 `generate_live_html.py` 脚本，将 Markdown 辩论流提取并生成精美的单文件 HTML 画布展示；
  * 实现气泡聊天、看多/看空颜色标签、组合经理金牌印章。
* **第 11~14 天：SQLite 统一存储与每日回测 AI 反思闭环**
  * 设计并初始化本地 SQLite 数据库表结构，将各种研判报告及推荐记录统一写入 SQLite 数据库，实现免文件依赖的历史检索；
  * 编写 `auto_backtest_audit.py` 追踪 3~5 日收益率，从 SQLite 读取历史研判进行自动化复盘与 AI 反思闭环。

---
*注：本零成本 MVP 方案已写入 `mvp_upgrade_plan.md`，它与商业级 `upgrade_plan.md` 互为补充，可直接作为近期开发和验证的最速执行路线图。*
